import { StyleSheet } from 'react-native';

export const general = StyleSheet.create({
  ContainerCenter: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  Padding: {
    padding: 16,
  },
});
